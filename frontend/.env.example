# Copy to .env.local for local overrides, or set in Vercel → Environment Variables.
# No trailing slash. Omit or leave empty when using `npm run dev` + Vite proxy.
VITE_API_URL=https://oncall-rca-api.onrender.com
